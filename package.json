{
  "name": "google-flex-auth-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@8.9.2",
  "scripts": {
    "dev": "pnpm --filter example dev",
    "build": "pnpm --filter google-flex-auth build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
